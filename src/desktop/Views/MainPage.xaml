<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:CajuAjuda.Desktop.ViewModels"
             xmlns:models="clr-namespace:CajuAjuda.Desktop.Models"
             x:Class="CajuAjuda.Desktop.Views.MainPage"
             x:DataType="viewModels:MainViewModel"
             Title="Dashboard - Chamados"
             BackgroundColor="{StaticResource PageBackground}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Atualizar" Command="{Binding LoadDataCommand}"/>
    </ContentPage.ToolbarItems>

    <Grid>
        <ActivityIndicator IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Color="{StaticResource Primary}"
                           VerticalOptions="Center"
                           HorizontalOptions="Center"
                           WidthRequest="60"
                           HeightRequest="60"/>

        <ScrollView IsVisible="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}">
            <VerticalStackLayout Spacing="15" Padding="20">

                <Border BackgroundColor="{StaticResource CardBackground}"
                        Padding="5"
                        StrokeThickness="1"
                        Stroke="{StaticResource Gray200}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10"/>
                    </Border.StrokeShape>

                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="5">
                        <Button Grid.Column="0" 
                                Text="Disponiveis"
                                Clicked="OnTab0Clicked"
                                BackgroundColor="{StaticResource Primary}"
                                TextColor="White"
                                FontAttributes="Bold"
                                CornerRadius="8"
                                HeightRequest="45"/>

                        <Button Grid.Column="1" 
                                Text="Em Andamento"
                                Clicked="OnTab1Clicked"
                                BackgroundColor="{StaticResource Gray200}"
                                TextColor="{StaticResource Gray700}"
                                FontAttributes="Bold"
                                CornerRadius="8"
                                HeightRequest="45"/>

                        <Button Grid.Column="2" 
                                Text="Fechados"
                                Clicked="OnTab2Clicked"
                                BackgroundColor="{StaticResource Gray200}"
                                TextColor="{StaticResource Gray700}"
                                FontAttributes="Bold"
                                CornerRadius="8"
                                HeightRequest="45"/>
                    </Grid>
                </Border>

                <CollectionView ItemsSource="{Binding ChamadosDisponiveis}"
                                IsVisible="{Binding SelectedTab, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}"
                                SelectionMode="None">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout VerticalOptions="Center" 
                                             HorizontalOptions="Center"
                                             Padding="60">
                            <Label Text="Nenhum chamado disponivel" 
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Gray600}"
                                   HorizontalOptions="Center"/>
                            <Label Text="Todos os chamados ja foram atribuidos" 
                                   FontSize="14"
                                   TextColor="{StaticResource Gray500}"
                                   HorizontalOptions="Center"
                                   Margin="0,8,0,0"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Chamado">
                            <Border BackgroundColor="{StaticResource CardBackground}"
                                    Padding="18"
                                    Margin="0,0,0,12"
                                    StrokeThickness="1"
                                    Stroke="{StaticResource Gray200}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>

                                <VerticalStackLayout Spacing="12">
                                    <Label Text="{Binding Titulo}"
                                           FontSize="17"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource Gray900}"/>

                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="Cliente:"
                                               FontSize="14"
                                               TextColor="{StaticResource Gray600}"/>
                                        <Label Text="{Binding NomeCliente}"
                                               FontSize="14"
                                               FontAttributes="Bold"
                                               TextColor="{StaticResource Gray700}"/>
                                    </HorizontalStackLayout>

                                    <HorizontalStackLayout Spacing="12">
                                        <Border Padding="8,4"
                                                BackgroundColor="{StaticResource Gray100}"
                                                StrokeThickness="0">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="6"/>
                                            </Border.StrokeShape>
                                            <Label Text="{Binding DataCriacao, StringFormat='{0:dd/MM/yyyy}'}"
                                                   FontSize="13"
                                                   TextColor="{StaticResource Gray700}"/>
                                        </Border>

                                        <Border Padding="8,4"
                                                BackgroundColor="{StaticResource Tertiary}"
                                                StrokeThickness="0">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="6"/>
                                            </Border.StrokeShape>
                                            <Label Text="{Binding Prioridade}"
                                                   FontSize="13"
                                                   FontAttributes="Bold"
                                                   TextColor="White"/>
                                        </Border>
                                    </HorizontalStackLayout>

                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,8,0,0">
                                        <Button Grid.Column="0"
                                                Text="Ver Detalhes"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=OpenChamadoCommand}"
                                                CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource Gray200}"
                                                TextColor="{StaticResource Gray800}"
                                                FontSize="14"
                                                FontAttributes="Bold"
                                                CornerRadius="8"
                                                HeightRequest="42"/>

                                        <Button Grid.Column="1"
                                                Text="Assumir"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=AtribuirChamadoCommand}"
                                                CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource Primary}"
                                                TextColor="White"
                                                FontSize="14"
                                                FontAttributes="Bold"
                                                CornerRadius="8"
                                                HeightRequest="42"/>
                                    </Grid>
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <CollectionView ItemsSource="{Binding MeusChamados}"
                                IsVisible="{Binding SelectedTab, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"
                                SelectionMode="None">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout VerticalOptions="Center" 
                                             HorizontalOptions="Center"
                                             Padding="60">
                            <Label Text="Nenhum chamado em andamento" 
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Gray600}"
                                   HorizontalOptions="Center"/>
                            <Label Text="Assuma chamados disponiveis para comecar" 
                                   FontSize="14"
                                   TextColor="{StaticResource Gray500}"
                                   HorizontalOptions="Center"
                                   Margin="0,8,0,0"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Chamado">
                            <Border BackgroundColor="{StaticResource CardBackground}"
                                    Padding="18"
                                    Margin="0,0,0,12"
                                    StrokeThickness="2"
                                    Stroke="{StaticResource Primary}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=OpenChamadoCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>

                                <VerticalStackLayout Spacing="10">
                                    <Label Text="{Binding Titulo}"
                                           FontSize="17"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource Gray900}"/>

                                    <Border Padding="8,4"
                                            BackgroundColor="{StaticResource Primary}"
                                            StrokeThickness="0"
                                            HorizontalOptions="Start">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="6"/>
                                        </Border.StrokeShape>
                                        <Label Text="{Binding Status}"
                                               FontSize="12"
                                               FontAttributes="Bold"
                                               TextColor="White"/>
                                    </Border>

                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="Cliente:"
                                               FontSize="14"
                                               TextColor="{StaticResource Gray600}"/>
                                        <Label Text="{Binding NomeCliente}"
                                               FontSize="14"
                                               FontAttributes="Bold"
                                               TextColor="{StaticResource Gray700}"/>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <CollectionView ItemsSource="{Binding ChamadosFechados}"
                                IsVisible="{Binding SelectedTab, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}"
                                SelectionMode="None">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout VerticalOptions="Center" 
                                             HorizontalOptions="Center"
                                             Padding="60">
                            <Label Text="Nenhum chamado fechado" 
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Gray600}"
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Chamado">
                            <Border BackgroundColor="{StaticResource Gray50}"
                                    Padding="18"
                                    Margin="0,0,0,12"
                                    StrokeThickness="1"
                                    Stroke="{StaticResource Gray200}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=OpenChamadoCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>

                                <VerticalStackLayout Spacing="10">
                                    <Label Text="{Binding Titulo}"
                                           FontSize="17"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource Gray700}"/>

                                    <Border Padding="8,4"
                                            BackgroundColor="{StaticResource Gray400}"
                                            StrokeThickness="0"
                                            HorizontalOptions="Start">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="6"/>
                                        </Border.StrokeShape>
                                        <Label Text="{Binding Status}"
                                               FontSize="12"
                                               FontAttributes="Bold"
                                               TextColor="White"/>
                                    </Border>

                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="Cliente:"
                                               FontSize="14"
                                               TextColor="{StaticResource Gray600}"/>
                                        <Label Text="{Binding NomeCliente}"
                                               FontSize="14"
                                               TextColor="{StaticResource Gray700}"/>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>