<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:CajuAjuda.Desktop.ViewModels"
             x:Class="CajuAjuda.Desktop.Views.LoginPage"
             x:DataType="viewModels:LoginViewModel"
             BackgroundColor="{StaticResource PageBackground}"
             Shell.NavBarIsVisible="False">

    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="40" 
                                 Spacing="30" 
                                 VerticalOptions="Center"
                                 MaximumWidthRequest="450"
                                 HorizontalOptions="Center">

                <VerticalStackLayout Spacing="12" Margin="0,0,0,20">
                    <Label Text="CAJU AJUDA"
                           FontSize="36"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}"
                           HorizontalOptions="Center"/>
                    <Label Text="Sistema de Suporte"
                           FontSize="16"
                           TextColor="{StaticResource Gray600}"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>

                <Border BackgroundColor="{StaticResource CardBackground}"
                        Padding="35"
                        StrokeThickness="1"
                        Stroke="{StaticResource Gray200}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="12"/>
                    </Border.StrokeShape>

                    <VerticalStackLayout Spacing="20">
                        <Label Text="Login"
                               FontSize="24"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Gray900}"/>

                        <VerticalStackLayout Spacing="8">
                            <Label Text="Email"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Gray700}"/>
                            <Entry Placeholder="seu.email@exemplo.com"
                                   Text="{Binding Email}"
                                   Keyboard="Email"
                                   FontSize="15"
                                   HeightRequest="50"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="8">
                            <Label Text="Senha"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Gray700}"/>
                            <Entry Placeholder="Digite sua senha"
                                   Text="{Binding Senha}"
                                   IsPassword="True"
                                   FontSize="15"
                                   HeightRequest="50"/>
                        </VerticalStackLayout>

                        <Button Text="ENTRAR"
                                Command="{Binding LoginCommand}"
                                BackgroundColor="{StaticResource Primary}"
                                TextColor="White"
                                FontAttributes="Bold"
                                FontSize="16"
                                HeightRequest="55"
                                CornerRadius="8"
                                Margin="0,15,0,0"/>

                        <ActivityIndicator IsRunning="{Binding IsBusy}"
                                           IsVisible="{Binding IsBusy}"
                                           Color="{StaticResource Primary}"
                                           HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Border>

                <Label Text="2025 Caju Ajuda - Todos os direitos reservados"
                       FontSize="12"
                       TextColor="{StaticResource Gray500}"
                       HorizontalOptions="Center"
                       Margin="0,20,0,0"/>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
