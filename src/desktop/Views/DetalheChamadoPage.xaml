<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CajuAjuda.Desktop.Views.DetalheChamadoPage"
             xmlns:viewModels="clr-namespace:CajuAjuda.Desktop.ViewModels"
             x:DataType="viewModels:DetalheChamadoViewModel"
             Title="{Binding Titulo}">

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="20">

            <Frame Padding="15" CornerRadius="10" BorderColor="LightGray">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Detalhes do Chamado" FontSize="20" FontAttributes="Bold" />

                    <Label>
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="Título: " FontAttributes="Bold" />
                                <Span Text="{Binding Titulo}" />
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>

                    <Label>
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="Status: " FontAttributes="Bold" />
                                <Span Text="{Binding Status}" />
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>